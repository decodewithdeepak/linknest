# Database Configuration (REQUIRED)
# Get your PostgreSQL connection string from NeonDB (https://neon.tech)
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# Authentication Secret (REQUIRED)
# Generate a secure random secret for JWT token signing
# You can generate one using: openssl rand -base64 32
# NEVER use a weak secret in production!
AUTH_SECRET=your-secure-random-secret-key-here

# Node Environment
NODE_ENV=development